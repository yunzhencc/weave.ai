# AGENTS.md

weave.ai 是基于 React、TypeScript、TanStack Start 和 TanStack AI 的 AI 创作应用，当前使用 Node.js 服务端与本地 SQLite 保存生成任务。

本文档保存常驻开发约定，详细说明见 `docs/`。在不违反系统与开发者指令的前提下，用户当前明确要求优先于本文档的一般约定。

## 基本原则

- 先读现有代码，再动手修改，优先沿用项目已有结构和写法。
- 使用中文沟通和编写项目说明，代码标识符、命令和 API 名称保持原样。
- 用满足需求的最少代码实现；先复用现有代码、原生能力和已安装依赖，不为假设需求引入抽象。
- 标准格式、协议、解析、压缩、加密、日期等通用能力优先使用原生 API 或成熟库，不手写已有可靠实现的底层能力。
- 不修改无关文件，不覆盖或回滚用户已有改动。生成文件由对应工具更新。
- 常规实现细节自行判断；只有缺失信息会实质改变结果时才提问，并继续推进不依赖答案的工作。
- 提交仅包含当前授权范围；“提交代码”表示本地提交，推送、发布等外部动作需有明确授权。
- 调整超时、重试、并发或大小限制时，说明适用环节、取值依据、影响和失败处理；涉及尚未明确的产品取舍时先确认，已有明确授权不重复询问。
- 用户明确要求长期保留的约束，整理为可执行规则并写入对应章节或专题文档；先检查已有规则，不把一次性要求自动升级为永久约束。

## 常用命令

使用 Node.js 24+，pnpm 版本以 `package.json` 的 `packageManager` 为准。

| 命令 | 用途 |
| --- | --- |
| `pnpm install` | 安装依赖 |
| `pnpm dev` | 启动开发服务，默认端口 3000 |
| `pnpm test` | 使用 Vitest 运行全部测试 |
| `pnpm typecheck` | TypeScript 类型检查 |
| `pnpm build` | 构建客户端与服务端 |
| `pnpm generate-routes` | 生成路由树 |

## 项目结构

按业务职责组织代码，域内再区分浏览器可用代码和服务端实现。当前目录：

```text
src/
  routes/       页面和 HTTP 路由入口、根布局
  components/   通用 UI、动效、展示组件
  lib/          无业务归属的工具和通用 hooks
  models/       模型目录、请求校验、供应商适配及现有任务存储
  stills/       图片请求构造与生成
  studio/       工作台业务、生成编排、视频提交与查询
  server/       当前 AI HTTP 接口处理与集成测试
docs/           架构、接入和任务生命周期说明
```

目录职责与后续代码放置规则见 [架构说明](docs/architecture.md)，不要提前创建没有实际代码的领域目录。

## 核心边界

- 业务界面与 hooks 归所属领域的 `ui/`；通用展示组件归 `components/`。路由负责参数和组装，不堆积生成业务。
- 领域根目录保持客户端安全；密钥、数据库、供应商请求放在 `server/`。浏览器代码不得直接导入服务端实现。
- SQL 留在 `server/db/`，HTTP 层负责鉴权、请求解析和响应，生成服务不依赖 `Request`、`Response`。
- 供应商密钥仅在服务端使用，不加 `VITE_` 前缀，不写入日志或响应。当前 `AI_API_TOKEN` 不是用户登录或多租户授权。
- 付费生成结果不确定时，不自动重新提交；查询已有任务必须沿用保存的渠道、端点和供应商任务 ID。

## 前端约定

- 优先复用现有 Base UI / shadcn、beUI 组件和 hooks，不新增仅改名或简单转发 props 的包装组件。
- 页面文案使用中文，图标优先使用 `lucide-react`；样式复用项目主题变量，图片默认保留原始比例。
- 状态按实际使用范围放置，复用动作与共享状态分开；组件、样式和状态的详细规则见 [前端规范](docs/frontend.md)。

## 按任务读取文档

| 涉及内容 | 开始修改前读取 |
| --- | --- |
| 新增页面、模块、跨域调用或调整代码位置 | [架构说明](docs/architecture.md) |
| 组件、页面文案、主题样式、状态或交互 | [前端规范](docs/frontend.md) |
| 模型、供应商、请求参数、密钥或 AI 接口 | [模型接入](docs/model-integration.md) |
| 任务状态、幂等、重试、超时、持久化或恢复 | [生成任务生命周期](docs/generation-lifecycle.md) |
| 本地启动和 Qwen 调用示例 | [README](README.md) |
| 代码审查、PR 处理或合并 | [代码审查与 PR 处理](docs/code-review.md) |

`docs/research/` 是历史调研，不作为当前已实现能力或供应商可用性的证明。改动使现行文档失实时，同步更新对应章节，避免多处重复维护同一规则。

## 验证与交付

- 验证与改动成比例：文档改动检查内容、路径和差异；代码改动运行相关测试及必要的类型检查或构建，通过后停止。
- 测试统一使用 Vitest，文件命名为 `src/**/*.test.{ts,tsx}`，默认在 Node 环境运行；同一文件内修改全局 fetch 或环境变量的用例保持串行，并恢复 mock 和环境变量。测试放置与隔离规则见 [架构说明](docs/architecture.md#测试与文档维护)。
- 模拟上游的测试、类型检查、构建、浏览器验证和真实模型调用分别报告；HTTP 200 或密钥已配置不能证明模型调用成功。
- 未经授权不为验证而发起付费模型请求。验证受阻或未执行时如实说明。
- 浏览器验证使用独立测试页面，不关闭用户已有窗口或标签页；只清理由本次任务启动的测试资源，不停止用户已有服务。
- 完成前检查 `git diff --check` 和改动范围，简述结果、验证情况及实际限制。
